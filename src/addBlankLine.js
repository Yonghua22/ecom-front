function addBlankLine() {
    var container = document.getElementById('blankSpaceContainer');
    var blankLine = document.createElement('div');
    blankLine.style.height = '50px'; // 设置空白行的高度
    container.appendChild(blankLine);
}

